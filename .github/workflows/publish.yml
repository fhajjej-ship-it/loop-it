name: Publish

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v6
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"
      - name: Resolve package version
        id: package
        run: |
          version=$(node -p "require('./package.json').version")
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Package version: $version"
      - name: Validate release tag
        if: github.event_name == 'release'
        run: |
          expected="v${{ steps.package.outputs.version }}"
          actual="${{ github.event.release.tag_name }}"
          if [ "$actual" != "$expected" ]; then
            echo "Release tag $actual does not match package version $expected" >&2
            exit 1
          fi
      - name: Check npm version
        id: npm
        run: |
          if npm view "@fhajjej/loop-it@${{ steps.package.outputs.version }}" version >/tmp/loop-it-npm-version 2>/dev/null; then
            echo "published=true" >> "$GITHUB_OUTPUT"
            echo "Version already published: $(cat /tmp/loop-it-npm-version)"
          else
            echo "published=false" >> "$GITHUB_OUTPUT"
            echo "Version is not published yet."
          fi
      - name: Validate npm publishing token
        if: steps.npm.outputs.published == 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if [ -z "$NODE_AUTH_TOKEN" ]; then
            echo "NPM_TOKEN GitHub Actions secret is missing or empty." >&2
            echo "Add a granular npm automation token with publish access to this repository's Actions secrets." >&2
            echo "If GitHub publishing is unavailable, follow RELEASE.md's manual fallback instead." >&2
            exit 1
          fi
      - run: npm run check
      - name: Publish to npm
        if: steps.npm.outputs.published == 'false'
        run: npm publish --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
